mode: append
items:
- Add milestone mapping or executable queue work for The repo exists, but `Chummer.Media.Contracts`
  is not yet real as the canonical render-only package plane..
- Publish or append runnable backlog for The repo exists, but `Chummer.Media.Contracts`
  is not yet real as the canonical render-only package plane..
- Finish milestone coverage modeling for media-factory so ETA and completion truth
  are no longer partial.
- Add milestone mapping or executable queue work for Media DTOs still need to be split
  cleanly between downstream render/job/asset lifecycle contracts and upstream narrative-authoring,
  delivery, and campaign-context contracts..
- Publish or append runnable backlog for Media DTOs still need to be split cleanly
  between downstream render/job/asset lifecycle contracts and upstream narrative-authoring,
  delivery, and campaign-context contracts..
- Add milestone mapping or executable queue work for The asset kernel still needs
  manifests, binary storage, render jobs, previews, TTL, retention, and lineage before
  renderer-specific pipelines move here..
- Publish or append runnable backlog for The asset kernel still needs manifests, binary
  storage, render jobs, previews, TTL, retention, and lineage before renderer-specific
  pipelines move here..
- Add milestone mapping or executable queue work for Run-services still owns the effective
  media seam, so this repo is scaffold-stage rather than a live render-only boundary..
- Publish or append runnable backlog for Run-services still owns the effective media
  seam, so this repo is scaffold-stage rather than a live render-only boundary..
