mode: append
items:
- "MF-005: Write service seam doc that defines media-factory ownership vs upstream ownership in `chummer.run-services` and downstream consumers (`hub-registry`, `presentation`, `play`). Include explicit out-of-scope rules/session relay/lore/provider-routing exclusions."
- "MF-005: Add an integration handoff checklist for render request ingress, approval-state persistence handoff, and asset metadata egress so seam ownership can be verified during extraction."
- "EXTRACT-001: Finalize `Chummer.Media.Contracts` package metadata and namespace policy as the canonical render-only contract plane; verify no narrative-authoring or campaign/session context DTOs exist in this repo."
- "EXTRACT-002: Add queue/asset lifecycle invariants doc covering dedupe key/scope ownership, retry timing, and approval/persist/reject terminal-state expectations."
- "EXTRACT-003: Add executable backlog slice for asset kernel completion (manifest persistence wiring, binary storage adapter seam, preview linkage, TTL/retention sweep contract, lineage traversal contract)."
- "EXTRACT-004: Define renderer move-in sequence after asset kernel completion: deterministic documents first, portraits second, video third, with dependency gates per stage."
- "EXTRACT-005: Capture scaffold-stage exit criteria check that proves run-services no longer owns the effective media seam for render-only jobs and asset lifecycle."
